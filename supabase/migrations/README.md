# Supabase v2 migrations

يتم تطبيق الترحيلات بالترتيب بعد أخذ نسخة احتياطية وتأكيد عملية استبدال مخطط `public` القديم. جدول `auth.users` محفوظ ولا يُحذف.

| الترحيل | الغرض |
|---|---|
| `000_reset_legacy_public.sql` | حذف مخطط `public` القديم فقط مع إبقاء Auth محفوظًا. |
| `001_v2_schema.sql` | إنشاء الجداول والعلاقات والأدوار والصلاحيات وRLS والدوال الأساسية. |
| `002_v2_security_fixes.sql` | استكمال سياسات RLS ودوال الاعتماد الذرية. |
| `003_v2_advisor_cleanup.sql` | تنظيف صلاحيات الدوال العامة غير المقصودة. |
| `004_v2_authenticated_grants.sql` | منح صلاحيات SQL اللازمة للمستخدمين المسجلين مع إبقاء anon بلا صلاحيات. |
| `005_attendance_actions.sql` | إضافة زر الحضور والانصراف الذري مع تسجيل الزمن وحساب `working_minutes`. |
| `006_incentives_payroll_automation.sql` | إنشاء الحوافز وكشف المرتبات التلقائي وربط خصم السلفيات عند اعتماد الفترة. |
| `007_incentive_batch_rpc.sql` | توزيع حافز على عامل محدد أو كل العمال النشطين بدفعة واحدة. |
| `008_pos_sale_draft.sql` | نقطة بيع ذرية تحفظ الفاتورة وبنودها وتربط المخزن والدفع والعميل وتحسب الإجمالي. |
| `009_audit_coverage.sql` | توسيع سجل التدقيق ليشمل الحضور والسلفيات والحوافز والمرتبات والخزنة والبنوك والمستخدمين والمنتجات. |

الدوال من نوع `SECURITY DEFINER` التي تستدعى عبر RPC مقصودة ومحمية بفحص `auth.uid()` والصلاحية داخل الدالة. يظهر لها تحذير Supabase لأن الدور `authenticated` يستطيع استدعاءها، وهذا مطلوب لتشغيل الاعتمادات والحضور من الواجهة. يظل تفعيل حماية كلمات المرور المسرّبة من إعدادات Auth خطوة تشغيلية موصى بها.
