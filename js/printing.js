(() => {
  'use strict';
  const esc = value => String(value ?? '').replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#039;','"':'&quot;'}[c]));
  const currentRoute = () => location.hash.replace(/^#\/?/, '').split('?')[0] || 'dashboard';
  const routeNames = { dashboard:'لوحة التحكم', guide:'دليل التشغيل', products:'المنتجات', inventory:'المخزون', production:'الإنتاج', sales:'المبيعات ونقطة البيع', returns:'المرتجعات والتالف', suppliers:'الموردون والمشتريات', treasury:'الخزائن والبنوك', expenses:'المصروفات', payroll:'المرتبات والسلف والحوافز', journal:'دفتر اليومية', reports:'التقارير التشغيلية', 'financial-statements':'القوائم المالية', 'bank-reconciliation':'التسوية البنكية', attendance:'الحضور والانصراف', employees:'الموظفون', users:'المستخدمون', roles:'الأدوار والصلاحيات', audit:'سجل التدقيق', settings:'الإعدادات', 'my-permissions':'صلاحياتي' };
  const identity = () => { let stored = {}; try { stored = JSON.parse(localStorage.getItem(window.APP_CONFIG?.settingsKey || 'sandal_factory_settings_v1') || '{}'); } catch {} return { name: stored.factoryName || window.APP_CONFIG?.factoryName || 'مصنع الصندل للأوعية البلاستيكية', number: stored.factoryNumber || '', address: stored.address || '', phone: stored.phone || '', email: stored.email || '', currency: stored.currency || window.APP_CONFIG?.currency || 'ج.س' }; };
  function toast(text, type = 'ok') { const region = document.getElementById('toast-region'); if (!region) return; const el = document.createElement('div'); el.className = `toast ${type === 'error' ? 'error' : ''}`; el.textContent = text; region.append(el); setTimeout(() => el.remove(), 4200); }
  function cloneForPrint(source) {
    const clone = source.cloneNode(true);
    clone.querySelectorAll('button,input,select,textarea,.header-actions,.table-tools,.form-footer,.modal-backdrop,.gx-help-fab,.gx-help-entry,.gx-help-drawer,.gx-onboarding,.gx-info-popover,.gx-tour-layer,[data-print-hide]').forEach(el => el.remove());
    clone.querySelectorAll('.panel-head').forEach(head => { head.querySelectorAll('.search,.btn,.badge:not(.blue)').forEach(el => el.remove()); });
    clone.querySelectorAll('.empty-state').forEach(el => { const b = el.querySelector('.gx-empty-help'); b?.remove(); });
    return clone;
  }
  function printNode(title, source) {
    if (!source) { toast('لا توجد بيانات ظاهرة للطباعة في هذه الصفحة.', 'error'); return; }
    const popup = window.open('', '_blank', 'width=980,height=900');
    if (!popup) { toast('اسمح بالنوافذ المنبثقة حتى تفتح نسخة الطباعة.', 'error'); return; }
    const id = identity(); const copy = cloneForPrint(source); const stamp = new Intl.DateTimeFormat('ar-SD', { dateStyle: 'full', timeStyle: 'short', timeZone: window.APP_CONFIG?.timezone || 'Africa/Khartoum' }).format(new Date());
    const contact = [id.number && `رقم المصنع: ${id.number}`, id.address, id.phone, id.email].filter(Boolean).join(' · ');
    popup.document.open(); popup.document.write(`<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>${esc(title)}</title><style>@page{size:A4;margin:14mm}*{box-sizing:border-box}html,body{margin:0;background:#fff;color:#142b38;font-family:Arial,'Tahoma',sans-serif;font-size:12px;line-height:1.7}body{direction:rtl}.print-sheet{max-width:190mm;margin:auto}.print-brand{display:flex;align-items:flex-start;justify-content:space-between;gap:24px;padding-bottom:14px;border-bottom:3px solid #0c958b}.print-brand h1{margin:0;color:#102c3c;font-size:22px;line-height:1.35}.print-brand p{margin:4px 0 0;color:#60757d;font-size:10px}.print-mark{display:grid;place-items:center;width:48px;height:48px;border-radius:13px;background:#0d7180;color:#fff;font-weight:800;font-size:15px}.print-meta{display:flex;justify-content:space-between;gap:12px;margin:12px 0 18px;color:#60757d;font-size:10px}.print-meta span{display:block}.print-title{margin:0 0 15px;color:#102c3c;font-size:20px}.print-content{width:100%}.print-content .page-header{display:none}.print-content .panel,.print-content .stat-card,.print-content .section-card{box-shadow:none;border:1px solid #dce7e8;border-radius:10px;margin:0 0 12px;padding:14px;break-inside:avoid}.print-content .dashboard-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.print-content .stats-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}.print-content .stat-card{padding:10px}.print-content .stat-top{font-size:9px}.print-content .stat-value{font-size:16px;margin:6px 0}.print-content .stat-foot{font-size:9px}.print-content .profit-card{background:#eef8f5;color:#142b38}.print-content .profit-card .panel-sub,.print-content .profit-caption{color:#60757d}.print-content .panel-title{font-size:14px;margin:0 0 8px}.print-content .panel-sub{font-size:9px;color:#60757d}.print-content .metric-line{display:flex;justify-content:space-between;gap:12px;padding:6px 0;border-bottom:1px solid #e6eeee}.print-content .metric-line:last-child{border-bottom:0}.print-content .data-table{width:100%;min-width:0;border-collapse:collapse;table-layout:auto}.print-content .data-table th,.print-content .data-table td{padding:7px 6px;text-align:right;border-bottom:1px solid #e2ecec;vertical-align:top;font-size:9px;overflow-wrap:anywhere}.print-content .data-table th{background:#eef8f5;color:#46636b;font-size:9px}.print-content .data-table tr:last-child td{border-bottom:0}.print-content .badge{display:inline-block;padding:2px 6px;border-radius:12px;background:#eef7f6;color:#3c6b6b;font-size:8px}.print-content .audit-list{display:grid;gap:9px}.print-content .audit-entry{border:1px solid #dce7e8;border-radius:9px;padding:10px;break-inside:avoid}.print-content .audit-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:7px}.print-content .audit-grid span{display:block;color:#60757d;font-size:8px}.print-content .audit-grid b{font-size:9px}.print-content .audit-details{border-top:1px solid #e2ecec;margin-top:8px;padding-top:7px}.print-content pre{white-space:pre-wrap;direction:ltr;text-align:left;font-size:8px}.print-footer{margin-top:18px;padding-top:9px;border-top:1px solid #dce7e8;color:#60757d;font-size:9px;display:flex;justify-content:space-between;gap:15px}.print-footer strong{color:#0d7180}@media print{.print-content a{color:inherit;text-decoration:none}.print-content .table-wrap{overflow:visible!important}.print-content .data-table{page-break-inside:auto}.print-content tr{page-break-inside:avoid;page-break-after:auto}}</style></head><body><main class="print-sheet"><header class="print-brand"><div><h1>${esc(id.name)}</h1><p>${esc(contact || 'بيانات المصنع تضاف من إعدادات المدير')}</p></div><div class="print-mark">SP</div></header><div class="print-meta"><span>تاريخ الطباعة: ${esc(stamp)}</span><span>المستخدم: ${esc(window.currentProfile?.full_name || 'المستخدم')}</span></div><h2 class="print-title">${esc(title)}</h2><section class="print-content">${copy.innerHTML}</section><footer class="print-footer"><span>${esc(id.email || id.phone || id.address || 'سجل داخلي للمصنع')}</span><strong>${esc(id.currency)}</strong></footer></main><script>window.onload=()=>setTimeout(()=>window.print(),120);window.onafterprint=()=>setTimeout(()=>window.close(),180);<\/script></body></html>`); popup.document.close();
  }
  function pageTitle() { return routeNames[currentRoute()] || document.querySelector('.page-header h1')?.textContent?.trim() || 'تقرير النظام'; }
  function printCurrentPage() { printNode(`طباعة ${pageTitle()}`, document.querySelector('#app .content')); }
  function printPanel(panel) { const title = panel.querySelector('.panel-title')?.textContent?.trim() || `طباعة ${pageTitle()}`; printNode(title, panel); }
  function addButtons() {
    if (!window.currentProfile || !document.querySelector('#app .page-header')) return;
    const route = currentRoute(); const actions = document.querySelector('#app .page-header .header-actions');
    if (actions && !actions.querySelector('[data-print-page]')) { const button = document.createElement('button'); button.type = 'button'; button.className = 'btn btn-outline btn-sm print-page-btn'; button.dataset.printPage = '1'; button.setAttribute('aria-label', `طباعة ${pageTitle()}`); button.textContent = ['reports','financial-statements','journal','audit','bank-reconciliation'].includes(route) ? 'طباعة التقرير' : 'طباعة الصفحة'; actions.append(button); }
    document.querySelectorAll('#app .panel').forEach(panel => { if (!panel.querySelector('.data-table,.audit-list,.metric-line,.stats-grid,.section-grid')) return; const head = panel.querySelector(':scope > .panel-head'); if (!head || head.querySelector('[data-print-panel]')) return; const b = document.createElement('button'); b.type = 'button'; b.className = 'btn btn-outline btn-sm print-panel-btn'; b.dataset.printPanel = '1'; b.textContent = 'طباعة هذا القسم'; head.append(b); });
  }
  document.addEventListener('click', e => { const pageButton = e.target.closest('[data-print-page]'); if (pageButton) { e.preventDefault(); printCurrentPage(); return; } const panelButton = e.target.closest('[data-print-panel]'); if (panelButton) { e.preventDefault(); printPanel(panelButton.closest('.panel')); } });
  const observer = new MutationObserver(() => { clearTimeout(window.__printMountTimer); window.__printMountTimer = setTimeout(addButtons, 60); });
  observer.observe(document.getElementById('app') || document.body, { childList: true, subtree: true });
  window.addEventListener('hashchange', () => setTimeout(addButtons, 120));
  setTimeout(addButtons, 220);
  window.PrintingEngine = { printPage: printCurrentPage, printPanel };
})();
